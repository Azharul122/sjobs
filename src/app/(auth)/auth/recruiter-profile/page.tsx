import RecruiterProfileProgress from "@/components/pages/auth/RecruiterProfile/RecruiterProfileProgress";
import React from "react";

const page = () => {
  return (
    <div>
      <RecruiterProfileProgress />
    </div>
  );
};

export default page;
